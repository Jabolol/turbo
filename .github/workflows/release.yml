name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        
    runs-on: ${{ matrix.os }}

    steps:
      - name: Setup repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends make gcc libc6-dev

      - name: Build Native Library
        run: make

      - name: Strip Native Library (Linux)
        if: runner.os == 'Linux'
        run: strip libturbo.so

      - name: Set description
        run: |
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "DESCRIPTION<<$EOF" >> "$GITHUB_ENV"
          $(git log --oneline --no-decorate $(git describe --tags --abbrev=0 @^)..@) >> "$GITHUB_ENV"
          echo "$EOF" >> "$GITHUB_ENV"

      - name: Release
        uses: softprops/action-gh-release@master
        if: ${{ github.ref == 'refs/heads/main' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "release draft"
          draft: true
          files: |
            libturbo.so
            libturbo.dylib
